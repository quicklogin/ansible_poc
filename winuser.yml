- name: install web server
  hosts: all
  tasks:
  - name: install web server
    win_feature:
      name: Web-Server
      state: present
      restart: yes
      include_sub_features: yes
      include_management_tools: yes
      
