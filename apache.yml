- hosts: all
  become: true
  tasks:
    - name: install the latest version of Apache
      yum:
        name: httpd
        state: latest
    - name: start apache server
      service:
        name: httpd
        state: started
    
